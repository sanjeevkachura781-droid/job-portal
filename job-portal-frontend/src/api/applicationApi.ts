import { api } from "./client";
import type { ApplicationStatus } from "../types/api";
export const applicationApi = { apply: (jobId: number, body: { resumeUrl?: string; coverLetter?: string }) => api.post(`/jobs/${jobId}/applications`, body), mine: () => api.get<{ applications: import("../types/api").Application[] }>("/applications/my"), recruiter: () => api.get<{ applications: import("../types/api").Application[] }>("/applications/recruiter"), forJob: (jobId: number) => api.get<{ applications: import("../types/api").Application[] }>(`/jobs/${jobId}/applications`), get: (id: number) => api.get<{ application: import("../types/api").Application }>(`/applications/${id}`), status: (id: number, status: ApplicationStatus) => api.patch(`/applications/${id}/status`, { status }), withdraw: (id: number) => api.patch(`/applications/${id}/withdraw`) };
