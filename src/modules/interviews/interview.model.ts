import { CreationOptional, DataTypes, InferAttributes, InferCreationAttributes, Model } from "sequelize";
import { sequelize } from "../../config/database";
export enum InterviewStatus { SCHEDULED="scheduled", COMPLETED="completed", CANCELLED="cancelled", RESCHEDULED="rescheduled" }
export class Interview extends Model<InferAttributes<Interview>, InferCreationAttributes<Interview>> { declare id: CreationOptional<number>; declare applicationId: number; declare recruiterId: number; declare candidateId: number; declare scheduledAt: Date; declare meetingLink: string | null; declare notes: string | null; declare status: CreationOptional<InterviewStatus>; }
Interview.init({ id: { type: DataTypes.INTEGER.UNSIGNED, autoIncrement: true, primaryKey: true }, applicationId: { type: DataTypes.INTEGER.UNSIGNED, allowNull: false }, recruiterId: { type: DataTypes.INTEGER.UNSIGNED, allowNull: false }, candidateId: { type: DataTypes.INTEGER.UNSIGNED, allowNull: false }, scheduledAt: { type: DataTypes.DATE, allowNull: false }, meetingLink: DataTypes.STRING(255), notes: DataTypes.TEXT, status: { type: DataTypes.ENUM(...Object.values(InterviewStatus)), defaultValue: InterviewStatus.SCHEDULED } }, { sequelize, tableName: "interviews", timestamps: true });
