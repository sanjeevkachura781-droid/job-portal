import { CreationOptional, DataTypes, InferAttributes, InferCreationAttributes, Model } from "sequelize";
import { sequelize } from "../../config/database";
export class Company extends Model<InferAttributes<Company>, InferCreationAttributes<Company>> { declare id: CreationOptional<number>; declare recruiterId: number; declare name: string; declare description: string | null; declare website: string | null; declare location: string | null; declare logoUrl: string | null; declare isApproved: CreationOptional<boolean>; declare createdAt: CreationOptional<Date>; declare updatedAt: CreationOptional<Date>; }
Company.init({ id: { type: DataTypes.INTEGER.UNSIGNED, autoIncrement: true, primaryKey: true }, recruiterId: { type: DataTypes.INTEGER.UNSIGNED, allowNull: false }, name: { type: DataTypes.STRING(150), allowNull: false }, description: DataTypes.TEXT, website: DataTypes.STRING(255), location: DataTypes.STRING(150), logoUrl: DataTypes.STRING(255), isApproved: { type: DataTypes.BOOLEAN, defaultValue: false }, createdAt: DataTypes.DATE, updatedAt: DataTypes.DATE }, { sequelize, tableName: "companies", timestamps: true, indexes: [{ fields: ["location"] }] });
